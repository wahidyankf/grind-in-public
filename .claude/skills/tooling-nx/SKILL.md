---
description: |-
  Guides Nx workspace work under the Nx standard: selecting projects by the graph, adding a target only where a capability exists, tracing a stale cached pass to its missing input, and answering a module-boundary finding. Use when adding a project or target to an Nx workspace, changing tags, target defaults, or named inputs, or when a cached or affected result looks wrong.
name: tooling-nx
---

Read .agents/skills/tooling-nx/SKILL.md completely, resolve every relative resource from that skill directory, and follow it as authoritative before acting.
