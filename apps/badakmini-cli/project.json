{
  "name": "badakmini-cli",
  "projectType": "application",
  "root": "apps/badakmini-cli",
  "sourceRoot": "apps/badakmini-cli",
  "targets": {
    "build": {
      "command": "go -C apps/badakmini-cli build -o dist/badak-mini ./cmd/badak-mini",
      "outputs": ["{projectRoot}/dist"]
    },
    "governance": {
      "cache": false,
      "command": "go -C apps/badakmini-cli run ./cmd/badak-mini harness instruction-size validate"
    },
    "markdown-links": {
      "cache": false,
      "command": "go -C apps/badakmini-cli run ./cmd/badak-mini harness markdown-links validate"
    },
    "capability-parity": {
      "cache": false,
      "command": "go -C apps/badakmini-cli run ./cmd/badak-mini harness capability-parity validate"
    },
    "rule-change": {
      "cache": false,
      "command": "go -C apps/badakmini-cli run ./cmd/badak-mini harness rule-change validate"
    },
    "lint": {
      "command": "go -C apps/badakmini-cli tool golangci-lint run"
    },
    "test:unit": {
      "command": "go -C apps/badakmini-cli test ./cmd/... ./internal/... ./tests/bdd ./tests/unit"
    },
    "test:coverage:unit": {
      "command": "mkdir -p local-tmp && go -C apps/badakmini-cli test -coverpkg=./internal/... -coverprofile=../../local-tmp/badakmini-unit.out ./cmd/... ./internal/... ./tests/bdd ./tests/unit && go -C apps/badakmini-cli tool cover -func=../../local-tmp/badakmini-unit.out | awk '/^total:/ { coverage=$3; sub(/%$/, \"\", coverage); printf \"unit statement coverage: %.1f%%\\n\", coverage; found=1; if (coverage + 0 < 99) exit 1 } END { if (!found) exit 1 }'",
      "inputs": [
        "default",
        "{workspaceRoot}/specs/apps/badakmini-cli/behavior/**/*.feature"
      ]
    },
    "test:coverage": {
      "command": "npm exec nx -- run badakmini-cli:test:coverage:unit"
    },
    "test:quick": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npm exec nx -- run badakmini-cli:typecheck",
          "npm exec nx -- run badakmini-cli:lint",
          "npm exec nx -- run badakmini-cli:test:unit",
          "npm exec nx -- run badakmini-cli:test:coverage"
        ],
        "parallel": false
      }
    },
    "typecheck": {
      "command": "go -C apps/badakmini-cli vet ./..."
    }
  }
}
