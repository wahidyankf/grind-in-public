# The message is sealed as typed input before the public-safety and Conventional
# Commit gates run. No forwarded hook arguments reach child commands.
exec ./hippo run --class ephemeral --resource-tier light --disk-path . -- \
  ./rhino gate run --surface commit-msg --message-file "$1"
