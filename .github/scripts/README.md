# GitHub Workflow Scripts

- [`test-resource-guard-bootstrap.sh`](test-resource-guard-bootstrap.sh) verifies checksum rejection,
  unsupported-platform failure, cache reuse, and concurrency-environment forwarding with a synthetic local release.
