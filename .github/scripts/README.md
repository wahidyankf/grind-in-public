# GitHub Workflow Scripts

- [`test-hippo-bootstrap.sh`](test-hippo-bootstrap.sh) verifies checksum rejection, unsupported-platform failure, cache
  reuse, and concurrency-environment forwarding with a synthetic local release.
